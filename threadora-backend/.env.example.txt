# SERVER CONFIG
PORT=3000
MONGODB_URI=mongodb://127.0.0.1:27017/threadora
JWT_SECRET=your_super_secret_jwt_key_here
FRONTEND_URL=http://localhost:5173

# CLOUDINARY (Media Storage)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# GOOGLE AUTH
GOOGLE_CLIENT_ID="your_google_client_id_here"

# EMAIL (Resend)
RESEND_API_KEY=your_resend_api_key_here

# RATE LIMITING
# Window duration in milliseconds (900000 = 15 minutes)
# Dev: leave unset (defaults to 10,000 req/window — effectively unlimited)
# Prod: 500 req per 15 min per IP — blocks bots/DDoS, invisible to real users
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=500

